<%= form_for(@handedness) do |f| %>
  <% if @handedness.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@handedness.errors.count, "error") %> prohibited this handedness from being saved:</h2>

      <ul>
      <% @handedness.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
