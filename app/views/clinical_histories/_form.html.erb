<%= form_for(@clinical_history) do |f| %>
  <% if @clinical_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clinical_history.errors.count, "error") %> prohibited this clinical_history from being saved:</h2>

      <ul>
      <% @clinical_history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label 'Patient ID' %><br />
    <%= f.text_field :patient_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
