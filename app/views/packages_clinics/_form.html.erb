<%= form_for(@packages_clinic) do |f| %>
  <% if @packages_clinic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@packages_clinic.errors.count, "error") %> prohibited this packages_clinic from being saved:</h2>

      <ul>
      <% @packages_clinic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
